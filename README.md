This project demonstrates the full pipeline requested in the assignment:

- Android Camera2 capture
- JNI bridge (Java ↔ C++)
- C++ native processing using OpenCV (Canny Edge Detection)
- Output converted to RGBA
- Rendered using OpenGL ES on Android
- Optional Web Viewer for processed frames

This repository contains:
- Android project structure
- Native C++ (JNI) sample function
- OpenCV + CMake wiring
- Simple Web viewer
